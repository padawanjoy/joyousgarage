export function Greeting() {
  return (
    <section className="greeting" aria-label="Greeting">
      <p>
        Notes from a garage where <em>code becomes craft</em>.
        <br />
        손으로 짓고, 글로 남깁니다.
      </p>
    </section>
  );
}
